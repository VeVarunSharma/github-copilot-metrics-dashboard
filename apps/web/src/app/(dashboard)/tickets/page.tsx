import { EmptyState } from '@/components/empty-state';
export default function Page() {
  return (
    <EmptyState
      title="Tickets — Coming Soon"
      description="This P2 surface is hidden from P0 navigation until ticket ingestion, privacy thresholds, formulas, demo data, and tests are ready."
    />
  );
}
