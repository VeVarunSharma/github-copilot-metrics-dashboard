import { EmptyState } from '@/components/empty-state';
export default function Page() {
  return (
    <EmptyState
      title="Teams — Coming Soon"
      description="This P2 surface is hidden from P0 navigation until team mappings, privacy thresholds, methodology, demo data, and tests are ready."
    />
  );
}
